version: 2
updates:
  # The only moving dependency in this repo is the pinned GitHub Actions used
  # by CI. The grammar itself is vendored (see UPSTREAM.md) and updated by hand
  # under supply-chain review, not by Dependabot.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Cooldown defers freshly-published action versions so a compromised release
    # has time to be yanked or flagged before Dependabot proposes it.
    cooldown:
      default-days: 7
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]
